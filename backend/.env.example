DATABASE_URL="mysql://root:@127.0.0.1:3306/cebasa"

JWT_SECRET="replace-with-a-long-random-secret"
CORS_ORIGIN="http://localhost:4200"

# Opcional: sin estas 4 variables, los emails (aviso de OC generada/enviada)
# quedan logueados en la consola del servidor en vez de enviarse.
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""
